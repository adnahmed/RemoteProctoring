# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RemoteProctoring-macOS-iPad' do
  platform :osx, '12.4'
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'KeychainAccess'
# pod 'CoreStore' , '~> 8.0'
  # Pods for RemoteProctoring
end

target 'RemoteProctoring-iPhone' do
  platform :ios, '16.0'
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'KeychainAccess'
# pod 'CoreStore' , '~> 8.0'
end
