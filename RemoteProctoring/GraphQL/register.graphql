mutation Register($username: String!, $password: String!, $role: String!, $givenName: String!, $email: EmailAddress!) {
  register(username: $username, password: $password, role: $role, given_name: $givenName, email: $email) {
    code
    token
    success
    message
    user {
      ...UserDetails
    }
  }
}
